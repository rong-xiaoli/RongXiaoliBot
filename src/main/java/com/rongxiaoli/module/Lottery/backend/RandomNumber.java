package com.rongxiaoli.module.Lottery.backend;

import java.security.SecureRandom;

public class RandomNumber {
    public double logRandom() {
        SecureRandom random = new SecureRandom();
        double baseNumber = random.nextDouble();
        // Todo: finish it.
        return 0;
    }
}
